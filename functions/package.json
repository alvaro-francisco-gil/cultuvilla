{
  "name": "cultuvilla-functions",
  "main": "dist/index.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.config.integration.mjs",
    "test:watch": "vitest",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test:all": "vitest run --config vitest.config.all.mjs",
    "test:coverage": "vitest run --coverage",
    "test:coverage:all": "vitest run --config vitest.config.all.mjs --coverage",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@cultuvilla/shared": "file:../packages/shared",
    "@vitest/coverage-v8": "^4.1.2",
    "esbuild": "^0.25.0",
    "eslint": "^9.39.4",
    "firebase-functions-test": "^3.5.0",
    "globals": "^17.6.0",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.60.0",
    "vitest": "^4.0.15"
  }
}
