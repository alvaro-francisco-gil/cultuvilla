node_modules/
.next/
dist/
.env
.env.local
.env.*.local
*.tsbuildinfo
.firebase/
.vercel
.env*.local

# Firebase emulator runtime logs
firestore-debug.log
firebase-debug.log
ui-debug.log
pubsub-debug.log
database-debug.log

# Local Claude Code worktree state
.claude/worktrees/

# Firebase service-account keys — never commit
# (canonical home is ~/.config/cultuvilla/<env>-sa.json, outside the repo)
*firebase-adminsdk*.json
*service-account*.json
*-sa.json

# Firebase CLI state — records which Google account the CLI is logged in as
.firebase-account

# Locally-downloaded escudos (coats of arms) — large, do not commit
scripts/data/escudos/
scripts/data/escudos-webp/
scripts/data/escudos-webp-thumb/
.firebase-account

# Active deep-link well-known files are copied at deploy time by
# apps/mobile/scripts/copy-well-known.mjs (per env), never committed.
apps/mobile/public/.well-known/apple-app-site-association
apps/mobile/public/.well-known/assetlinks.json

# Tooling worktrees
.claire/

# Subagent-driven-development scratch ledger
.superpowers/
